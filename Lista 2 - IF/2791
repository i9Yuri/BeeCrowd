# Read the four cup values separated by spaces
cups = list(map(int, input().split()))

# Find the index of the cup with value 1 (bean is present)
bean_cup = cups.index(1)

# Print the cup number (index + 1)
print(bean_cup + 1)
