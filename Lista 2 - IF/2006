# Get tea type and convert answers to a list
tea_type = int(input())
answers = list(map(int, input().split()))
correct_count = 0

for answer in answers:
    if answer == tea_type:
        correct_count += 1
    else:
        continue

# Print the result
print(correct_count)
