x = int(input())
y = int(input())

soma_impares = 0

inicio = min(x, y) + 1
fim = max(x, y)

for numero in range (inicio, fim):
    if numero % 2 != 0:
        soma_impares += numero
        
print(soma_impares)
